export const metadata = {
    title: "Email Splitter | Split Email Lists into Batches | EmailToolHub",
    description:
        "Effortlessly split large email lists into smaller, manageable batches. Organize and optimize email campaigns efficiently.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
