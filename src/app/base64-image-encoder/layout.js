export const metadata = {
    title: "Base64 Image Encoder | Convert Images to Base64 Instantly | EmailToolHub",
    description:
        "Convert images to Base64 format for HTML, CSS, JSON, and XML usage. Easily encode and copy Base64 image data.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
